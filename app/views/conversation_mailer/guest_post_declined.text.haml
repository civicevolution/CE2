- if @join_request == "accepted"
  :plain
    Your request to join "#{@conversation.title}" has just been accepted, but your guest comment has been declined

    #{@post.text}

- elsif @join_request == "declined"
  :plain
    Sorry, your guest comment and request to join "#{@conversation.title}" have both been declined

    #{@post.text}

- else
  :plain
    Your comment to "#{@conversation.title}" has just been accepted

    #{@post.text}


~''
~''
You can visit the conversation at #{@host}/#/conversation/#{@conversation.code}/#{@conversation.munged_title}


