%div
  %h4 Sorry, your comment to "#{@conversation.title}" has been declined

  %p Here is the comment that was declined:
  %blockquote
    #{@post.text}

  %p You can visit the conversation at #{@host}/#/conversation/#{@conversation.code}/#{@conversation.munged_title}
